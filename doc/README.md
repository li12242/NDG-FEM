
1. Setup the sphinx environment

```bash
pip install sphinxcontrib-matlabdomain sphinx-rtd-theme recommonmark sphinx-autobuild
```

2. Run following commands to build document with sphinx

```bash
sphinx-autobuild source build/html
```
